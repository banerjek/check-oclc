These files contain a simple example of how to use the WorldCat API to retrieve records and search for the presence of desired fields. 

You will need wskey.txt to contain a key that has full access permissions. Use of a limited key will not retrieve all fields.
